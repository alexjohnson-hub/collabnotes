rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Rules for the 'notes' collection
    match /notes/{noteId} {
      // Document-level access: A user can get, update, or delete a note if they are the owner.
      allow get, update, delete: if request.auth != null && resource.data.ownerId == request.auth.uid;
      
      // A user can create a note if the new note's ownerId is their own UID.
      allow create: if request.auth != null && request.resource.data.ownerId == request.auth.uid;
    }

    // Collection-level access: This rule allows a user to query the 'notes' collection.
    match /notes/{document=**} {
        // A user can list (query) notes if the query filters by their ownerId.
        allow list: if request.auth != null && request.query.filters.ownerId == request.auth.uid;
    }
  }
}
